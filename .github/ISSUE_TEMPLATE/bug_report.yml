name: "🐛 Bug Report / 问题反馈"
description: "Something isn't working? Let's fix it together! / 遇到问题了？一起来解决！"
title: "[Bug]: "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        ## 👋 Hey there! / 你好！
        
        Thanks for taking the time to report this issue! Before we dive in, a quick checklist:
        
        感谢你花时间反馈问题！在开始之前，快速检查一下：
        
        - [ ] I'm using the latest version (`kkp --version`) / 我用的是最新版本
        - [ ] I searched existing issues / 我搜索过已有的 issue

  - type: input
    id: version
    attributes:
      label: "📦 kkp version / 版本"
      description: "Run `kkp --version` / 运行 `kkp --version`"
      placeholder: "e.g. 1.1.1"
    validations:
      required: true

  - type: dropdown
    id: os
    attributes:
      label: "💻 Operating System / 操作系统"
      options:
        - Windows
        - macOS
        - Linux
        - Other
    validations:
      required: true

  - type: textarea
    id: what-happened
    attributes:
      label: "🤔 What happened? / 发生了什么？"
      description: "Tell us what you were trying to do and what went wrong / 告诉我们你想做什么，哪里出错了"
      placeholder: |
        I ran `kkp 3000` but...
        我运行了 `kkp 3000` 但是...
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: "✨ What did you expect? / 你期望的结果？"
      placeholder: |
        I expected the process to be killed...
        我期望进程被终止...

  - type: textarea
    id: debug-info
    attributes:
      label: "🔍 Debug info / 调试信息"
      description: |
        Please run the commands for your OS and paste the output / 请运行对应系统的命令并粘贴输出
      value: |
        <!-- 
        Run these commands and paste output below / 运行以下命令并粘贴输出：
        
        ALL SYSTEMS / 所有系统:
        kkp --list
        
        WINDOWS:
        netstat -ano | findstr <port>
        
        macOS:
        lsof -i :<port>
        
        Linux:
        ss -tlnp | grep <port>
        -->
        
        ```
        (paste output here / 粘贴输出到这里)
        ```
      render: markdown

  - type: textarea
    id: extra
    attributes:
      label: "📎 Anything else? / 还有什么要补充的？"
      description: "Screenshots, logs, your favorite emoji... / 截图、日志、你喜欢的表情..."
